spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fridgeStorage?useSSL=false
    username: root
    password: 'R1D2Ms$3!'
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect

#JPA Settings